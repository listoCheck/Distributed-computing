# Результаты локальной проверки, 14.09.2026

Сборка: локальный Docker-образ `pa3-legacy:local`, clang 3.5.

```
clang-3.5 -std=c99 -Wall -Wextra -Werror -pedantic main.c ipc.c clock.c mutex.c protocol.c -L. -lruntime -o pa4
```

Код возврата 0, предупреждений нет. Полученный бинарник проверен с оригинальной
библиотекой из `../lab3/pa3/libruntime.so` в образе `pa3-toolchain:local`.
Первый запуск пяти процессов также выполнен в старом образе; он выявил вывод
runtime в stderr, после чего добавлено перенаправление на время print.
Полные восемь сценариев ниже выполнены после этого исправления.

```
OK: p=1 mutex=False
OK: p=1 mutex=True
OK: p=2 mutex=False
OK: p=2 mutex=True
OK: p=5 mutex=False
OK: p=5 mutex=True
OK: p=15 mutex=False
OK: p=15 mutex=True
All 8 integration cases passed.
```

Проверены завершение, отсутствие stderr, полнота печати, барьеры, количество
каналов и трасса взаимного исключения. Содержимое официальных трёх заголовков
и библиотеки побайтово совпадает с исходными файлами предыдущей работы.
Проверена структура `pa4.tar.gz`: только 5 файлов C, 4 заголовка и Makefile,
все внутри `pa4/`. Проверка ботом не выполнялась.
